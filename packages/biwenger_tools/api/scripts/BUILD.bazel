load("@rules_python//python:defs.bzl", "py_binary")

# One-shot backfill of draft turn timings from Cloud Logging, for the picks
# made before the feature shipped. Dry-run by default; --write persists.
py_binary(
    name = "backfill_draft_timings",
    srcs = ["backfill_draft_timings.py"],
    main = "backfill_draft_timings.py",
    deps = ["//core:firestore"],
)
