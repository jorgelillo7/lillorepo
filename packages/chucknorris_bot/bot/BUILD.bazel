load("//tools/bazel:python_service.bzl", "python_service")

python_service(
    name = "bot",
    package = "chucknorris_bot",
    main = "app.py",
    repository = "europe-southwest1-docker.pkg.dev/biwenger-tools/biwenger-docker/chucknorris_bot",
    secrets = [".env"],
    deps = ["@pypi//requests"],
    extra_env = {
        "PORT": "8080",
        "PYTHONPATH": "/app",
    },
)
