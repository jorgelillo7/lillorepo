# MCP Setup

The MCP services are broadly the same across clients, but the wiring differs:

- Claude Code uses Claude-specific MCP configuration and commands.
- Codex uses `codex mcp` and `.codex/config.toml`.
