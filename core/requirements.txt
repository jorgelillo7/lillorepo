# core/requirements.txt
requests
google-api-python-client
google-auth-oauthlib
google-auth
google-cloud-firestore
python-dateutil
python-json-logger

# dev-only — everything below this marker is deliberately NOT installed into
# docker/Dockerfile.base (dead weight in Cloud Run). `scripts/check_base_sync.py`
# reads this exact marker, so keep the line and add dev tools underneath it.
# Herramientas de desarrollo (centralizadas aquí para todos los módulos)
black
flake8
pytest
requests-mock
freezegun
